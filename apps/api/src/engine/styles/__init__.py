"""Uslub bilimlar bazasi (docs/03-roadmap.md 2.8): taklif → sxema/kontrast tekshiruvi →
render darvozasi → kuchli model VisionQA → ``style_theme`` katalogi.

- ``schema`` — JSON Schema yuklash/tekshirish + WCAG kontrast tekshiruvi.
- ``proposer`` — LLM orqali yangi tema taklif qilish.
- ``gate`` — sxema + render CLI + VisionQA darvoza quvuri.
- ``registry`` — ``style_theme`` jadvali bilan ishlash (CRUD, gate, import, vault hujjat).
"""
